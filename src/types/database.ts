// Placeholder Database shape for the Supabase typed client. Replace with the
// real generated types (`supabase gen types typescript`) once the
// transactions/budgets schema exists in phase 2b.
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
};
