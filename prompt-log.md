# Prompt Log

| S.No | AI Tool     | Prompt Used                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Purpose                                                                           |
| ---- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- |
| 1    | Claude Code | Initialize a Vite + React + TypeScript project in this directory. Install and configure Tailwind CSS. Prove the config works with a styled test element. Initialize git.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Step 1a: project init and Tailwind config only                                    |
| 2    | Claude Code | Install react-router-dom, recharts, lucide-react. Create src/ folder structure (components, pages, lib, hooks, types) with placeholders. Set up ESLint + Prettier with sensible defaults and add lint/format npm scripts. Verify lint passes and dev server starts clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Step 1b: folder structure, locked-stack package installs, and lint/format tooling |
| 3    | Claude Code | Set up react-router-dom routes for /login, /signup, /dashboard, /transactions, /budgets with placeholder pages. Build a responsive sidebar layout shell (lucide-react icons, logout placeholder) with a deliberate finance-app visual identity (real palette/type pairing, not default Tailwind). Audit against Web Interface Guidelines (focus states, contrast, semantic nav, mobile drawer a11y) and fix what's flagged. Verify every route via Playwright, zero console errors.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Step 1c: routing and layout shell (final piece of Phase 1)                        |
| 4    | Claude Code | Create a Supabase client in src/lib/supabase.ts reading VITE_SUPABASE_URL/VITE_SUPABASE_ANON_KEY from env vars. Add .env.example with placeholders and confirm .env is gitignored. Add a minimal Database type placeholder in src/types/. Add a temporary connection-check on app load and verify it against a real Supabase project before removing it later.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Step 2a: Supabase client + env setup (no schema/auth yet)                         |
| 5    | Claude Code | Write SQL for transactions and budgets tables (exact columns specified), enable RLS with SELECT/INSERT/UPDATE/DELETE policies scoped to auth.uid(), explained per-policy for Q&A defensibility, plus indexes for the analytics query patterns. Update the Database TypeScript type to match the real schema and repoint the 2a connection-check at the real transactions table, expecting an empty array instead of a schema-cache-miss error.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Step 2b: database schema, RLS, and grants (no auth UI yet)                        |
| 6    | Claude Code | Build Login and Signup pages wired to real Supabase Auth (signInWithPassword, signUp), client-side validation with per-field inline errors, loading states, redirect to /dashboard on success. Apply frontend-design principles consistent with the existing ledger identity. Audit against Web Interface Guidelines and fix what's flagged (aria-live, name attrs, spellCheck, reduced-motion, curly quotes, per-field errors). Verify with Playwright: real signup, re-login in a fresh session, wrong-password error path, signup validation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Step 3a: Login/Signup pages wired to Supabase Auth (no protected routes yet)      |
| 7    | Claude Code | Protect all routes except /login and /signup using Supabase's real session state (not a hardcoded check), with a loading state during the initial auth check to avoid a flash of protected content. Wire the sidebar's logout button to signOut and redirect to /login. Handle session persistence across refresh. Audit against Web Interface Guidelines. Verify with Playwright: unauthenticated redirect for every protected route, refresh staying logged in, and logout re-protecting the dashboard.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Step 3b: protected routes, logout, session persistence (Phase 3 complete)         |
| 8    | Claude Code | Build the read-only Transactions list (useTransactions hook + table UI): loading skeleton, empty state, error state, real data from Supabase scoped by RLS. Apply frontend-design principles — a real HTML table styled as a ledger register, with the mono tabular-numeral signature reserved back in phase 1c now used for every amount. Audit against Web Interface Guidelines and fix what's flagged. Verify with Playwright: a seeded user's transactions render correctly, and a second, brand-new user sees zero rows (RLS actually scopes per-user, not just appears to).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Step 4a: transactions list/read view (create/edit/delete come in 4b/4c)           |
| 9    | Claude Code | Build the add-transaction form (amount, category, type, description, date) with per-field inline validation, real Supabase insert scoped to the user, list refresh via the 4a hook (no page reload), loading/disabled-button state, and a non-raw error message on insert failure. Apply frontend-design principles consistent with the ledger register. Audit against Web Interface Guidelines and fix what's flagged. Verify with Playwright: invalid input rejected with zero network requests to Supabase, valid submit updates the list with zero page navigations, and RLS still isolates the new row from a second user.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Step 4b: add-transaction form (edit/delete come in 4c)                            |
| 10   | Claude Code | Add edit and delete for existing transactions: row-level Edit/Delete actions, the add form reworked into a dual create/edit form, a real confirmation dialog for delete (never immediate) showing exactly what's being deleted. Extract the mobile nav drawer's focus-trap/dialog logic into a shared hook first, since the confirm dialog needs identical mechanics. Apply frontend-design principles. Audit against Web Interface Guidelines and fix what's flagged. Verify with Playwright (edit, delete with cancel/Escape/confirm, mobile drawer regression) and via direct API calls that RLS actually blocks a second user's UPDATE/DELETE on the first user's data, not just hides it in the UI.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Step 4c: edit and delete (Phase 4 / Transactions CRUD complete)                   |
| 11   | Claude Code | Build the complete Analytics Dashboard in one pass: summary cards (income/expenses/net savings, current month), a pie chart (expense by category, current month), a line chart (income vs expenses, last 6 months), 2-3 computed plain-language insights, graceful empty state for a new user. Apply frontend-design principles with explicit attention to chart accessibility (no color-only encoding). Audit against Web Interface Guidelines and fix what's flagged. Verify with Playwright: real Supabase data (exact figures, not placeholders), the empty state for a fresh user, and RLS isolation for a second user's dashboard.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Step 5: Analytics dashboard (Phase 5 complete)                                    |
| 12   | Claude Code | Build the complete Budgets feature in one pass: form to set/update a monthly limit per category as an upsert on the existing (user_id, category) unique constraint, a list showing limit vs actual spend this month with a progress bar and a binary over-budget rust state, empty/loading/error states. Apply frontend-design principles consistent with Dashboard/Transactions. Audit against Web Interface Guidelines and fix what's flagged. Verify with Playwright: correct spend calculation, over-budget state triggers on exceeding the limit, editing upserts rather than duplicating, and RLS isolation for a second user.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Step 6: Budgets (Phase 6 complete — all core features done)                       |
| 13   | Claude Code | Dedicated testing pass: negative/zero amount rejection for both the transaction amount and budget monthly-limit fields; simulate real Supabase failures via Playwright network interception (not just client-side validation) for transaction insert/update/delete and budget upsert, confirming clean inline errors, no raw backend leaks, no data corruption, and full recovery afterward; consolidated re-check of all three empty states for regressions. Found no application bugs — every path already handled correctly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Step 10: Testing pass (no code changes needed — verification only)                |
| 14   | Claude Code | Deploy: push to GitHub, deploy the frontend to Vercel, and set the Supabase env vars in Vercel's dashboard. Along the way, running the real `npm run build` (not just `tsc --noEmit`) surfaced two genuine TypeScript errors in Dashboard.tsx's Recharts Tooltip formatter props that had gone undetected all build — fixed by widening the formatter type with a runtime guard. Wrote a real README and a vercel.json SPA rewrite (needed since react-router-dom is client-side routing). Verified the live production deployment end-to-end with a real signup and empty-state check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Step 11: Deploy (Phase 11 complete — project fully live)                          |
| 15   | Claude Code | Extend the existing Dashboard summary cards (income/expenses/net savings) with a This Month / All Time toggle instead of building new ones, since the request overlapped with Phase 5's existing block. Relabeled cards to Income/Expenses/Balance, added a plain-language caption ("You're saving" / "Spending more than you earn") so the positive/negative state isn't color-only, and synced the toggle to a `?summary=` URL param per the Web Interface Guidelines audit finding. Verified with Playwright against real seeded multi-month transaction data: correct This Month vs. All Time totals, correct rust/negative visual state, and RLS isolation for a second user.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Post-deploy enhancement: Dashboard summary block (This Month / All Time)          |
| 16   | Claude Code | Set up the GitHub-to-Vercel auto-deploy connection (`npx vercel git connect`) so future pushes to main deploy automatically, instead of requiring a manual `vercel --prod` each time. Required linking GitHub as a Vercel login connection and separately granting the Vercel GitHub App repo access, both one-time account-level steps the user had to complete in their own browser.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Post-deploy: wire up GitHub → Vercel CI/CD                                        |
| 17   | Claude Code | Restyle Login/Signup backgrounds only to a dark "premium fintech dashboard" mood: deep navy base, soft orange-to-red radial glow behind the card, dark elevated card with a gradient top accent bar, scoped entirely to new `fintech-*` theme tokens so the rest of the app's light theme is untouched. Hand-verified WCAG contrast math for every color pair before building (the app's existing `rust` error color only hit ~2.6:1 on navy, so a brighter dark-safe red was chosen instead; the button gradient's endpoints were tuned so white text clears 4.5:1 at both ends). web-design-guidelines flagged missing `color-scheme: dark`, scoped to the AuthCard subtree only. Verified with Playwright: both pages render with zero console errors, and the rest of the app's light theme is confirmed unaffected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | UI enhancement: dark fintech theme for auth pages (scoped)                        |
| 18   | Claude Code | Extend the dark fintech theme from Login/Signup to the whole app. Redefined the base theme tokens (paper/ink/ledger/rust/line) directly to dark values instead of keeping a separate scoped token set, since the whole app is dark now — consolidated the auth pages off their one-off `fintech-*` tokens onto the same base tokens. This surfaced several real gotchas fixed along the way: Sidebar and AppLayout's mobile header relied on an inverted `bg-ink text-paper` trick for permanently-dark chrome, which would have silently broken (rendered as a white bar) once `ink`/`paper` swapped roles; two modal scrim overlays used `bg-ink/60` for a dark backdrop, which needed to become a theme-independent `bg-black/60`; Recharts' Tooltip has its own hardcoded white background unrelated to CSS variables, needing an explicit `backgroundColor` override; the pie chart's slice-separator stroke was hardcoded white; and the original `ledger` green/`rust` red, tuned for white-text-on-solid-button contrast under the old light theme, both failed contrast in that direction against the new bright/dark-safe values, so a separate deep-tone pair was introduced for button and delete-action backgrounds specifically. Verified with Playwright across Dashboard (both summary periods), Transactions (add form, table, delete dialog), Budgets (under- and over-budget states), and the mobile nav drawer — zero console errors throughout. | UI enhancement: extend dark fintech theme app-wide                                |
| 19   | Claude Code | Four new features in one pass: (1) Savings goals — new `goals` table/RLS, a Goals page mirroring the Budgets page's structure but with delete added, progress bars pulling from the existing all-time balance calculation. (2) Recurring transactions — a "Repeats monthly" checkbox, two new transactions columns (`is_recurring`, `recurring_parent_id`), and a client-side catch-up effect in `useTransactions` that backfills any months missed since the last logged instance on load, since this stack has no server-side cron. (3) A stacked bar chart on the Dashboard showing per-category expense trends over the last 6 months, reusing the existing pie chart's color/pattern system. (4) CSV export (client-side, no new dependency) and a printable monthly-report page that reuses the app's own color tokens, overriding them to a light palette scoped to `@media print` so the same markup renders correctly in both the dark app and a printed/PDF report. Verified with Playwright: goal CRUD, validation, and RLS isolation; recurring backfill correctness and idempotency (a second load doesn't duplicate); CSV content and the print-media color override; and a full cross-feature smoke test across every page with zero console errors.                                                                                                                                                                                                  | Add Savings Goals, Recurring Transactions, Category Trends, CSV/PDF Export        |
| 20   | Claude Code | Add a delete option to Budgets — the RLS delete policy already existed from the original schema, only the UI was missing it. Reused the same edit/delete icon pair and ConfirmDeleteDialog pattern already established for Transactions and Goals, rather than introducing a new pattern. Verified with Playwright: the confirm dialog shows the correct category and monthly limit, the budget is actually removed on confirm, and a second user's direct API delete attempt against the first user's budget returns a "successful" 204 with zero rows actually affected — RLS filtering the row out before the delete touches it, not an application-level check.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Budgets: add delete option                                                        |
| 21   | Claude Code | Add a "Reset all" bulk-delete option to Transactions and Budgets. Given the much larger blast radius of wiping an entire table versus deleting one row, built a dedicated ResetConfirmDialog distinct from the existing single-item ConfirmDeleteDialog — it shows the exact item count about to be destroyed and requires typing the word RESET before the confirm button enables, rather than a plain click-through confirm. Verified with Playwright: the confirm button is disabled by default and stays disabled on a near-miss ("resett"), enables only on the exact word (case-insensitive), Cancel leaves all data untouched, and confirming actually deletes everything and shows the correct empty state, for both Transactions and Budgets independently.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Transactions/Budgets: add "Reset all" bulk-delete with typed confirmation         |
| 22   | Claude Code | Add goal contributions: adding or editing an income transaction now shows an optional "Add to a goal" select listing the user's existing goals (or a hint linking to /goals if they have none yet). Goal progress on the Goals page switched from tracking the account's overall balance to summing the income transactions actually earmarked to it via a new goal_id column — a deliberate model change so a user can save toward several goals independently rather than every goal racing against the same shared balance. Recurring income keeps contributing automatically: each auto-generated monthly instance carries its template's goal_id forward. Verified with Playwright: the goal select is hidden for expense transactions and when no goals exist yet, two goals track their contributions completely independently, editing an income transaction correctly preselects and lets you reassign its goal (moving the contribution from one goal to the other), and three months of backfilled recurring income summed correctly toward its earmarked goal.                                                                                                                                                                                                                                                                                                                                                                                           | Goals: add income-to-goal contribution tracking                                   |
| 23   | Claude Code | Let the Category field on the transaction form accept free text instead of only a fixed dropdown. Used the native `<input list>` + `<datalist>` pattern rather than a custom combobox — a user can still pick one of the existing suggestions or type an entirely new category, with no extra library and no accessibility work needed since datalist is native HTML. Scoped to Transactions only, not Budgets, since a budget category needs to match an actual spending category to mean anything. Verified with Playwright: the fixed suggestions still appear, empty input is still rejected, a brand-new custom category is trimmed and saved correctly, and it flows through to the Dashboard's pie chart and category-trend chart with zero changes needed there since both already compute categories dynamically from the data.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Transactions: allow free-text category entry                                      |
| 24   | Claude Code | Extend the same free-text category input to Budgets, since keeping it Transactions-only left a real gap: a user couldn't set a budget against a custom category they'd already been logging transactions under. Suggestions now merge the fixed starter list with whatever categories actually appear in the user's own transactions, so a custom category like "Pet Supplies" is immediately budgetable and its spend total matches correctly. Verified with Playwright: the datalist includes both fixed and custom categories, a budget set against a custom category correctly tracks spend from a matching transaction, and empty-category validation still holds.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Budgets: allow free-text category entry, matched against transaction categories   |
| 25   | Claude Code | Add "scan a bill" to the transaction form: uploading a receipt image runs client-side OCR (Tesseract.js, dynamically imported so it doesn't bloat the main bundle) and heuristically pre-fills amount, date, category, and description, which the user reviews before saving — never auto-submitted blind. Chose on-device OCR over an AI vision API after discussing the tradeoff with the user (free, no new secret/backend infra, works within the existing Supabase-only stack, at the cost of lower accuracy than a vision model on messy receipts). Added a private Supabase Storage bucket with per-user-folder RLS policies for the uploaded images, plus a receipt_path column and a "view receipt" icon in the transactions table that opens a short-lived signed URL. Verified with a real (synthetic but realistic) receipt image run through the actual OCR pipeline: every field — amount, date, category, description — extracted correctly; confirmed the file lands in Storage, the signed-URL viewer works, and a second user is blocked (400) from both generating a signed URL and directly downloading the first user's receipt.                                                                                                                                                                                                                                                                                                                | Transactions: add bill upload with on-device OCR auto-fill                        |
